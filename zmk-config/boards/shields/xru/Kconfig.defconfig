if SHIELD_XRU_LEFT

config ZMK_KEYBOARD_NAME
    default "Xru0.2"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_XRU_LEFT || SHIELD_XRU_RIGHT

config ZMK_SPLIT
    default y

config USB_NRFX
    default y

endif

config BT_CTLR
    default BT

if USB

config USB_DEVICE_STACK
    default y

endif
